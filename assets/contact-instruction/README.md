Put the 5-second MAX profile sharing instruction video here as:

instruction.mp4

The Docker image copies this folder into /app/assets. By default the bot reads:

assets/contact-instruction/instruction.mp4

You can override the path with CONTACT_INSTRUCTION_VIDEO_PATH.
