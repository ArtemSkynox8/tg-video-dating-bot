Put the fortune wheel reveal video here as:

wheel.mp4

The bot uses this file by default before showing a random referral contact.
You can override the path with FORTUNE_WHEEL_VIDEO_PATH or provide an already uploaded MAX media token with FORTUNE_WHEEL_VIDEO_ID.
