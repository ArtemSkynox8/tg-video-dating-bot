Place recorder theme backgrounds here:

- `light.jpg` - background for light theme
- `dark.jpg` - background for dark theme

The recorder uses the matching image inside the circular camera area with a 15px blur.
